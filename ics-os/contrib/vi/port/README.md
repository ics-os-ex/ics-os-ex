# ICS-OS vi port shims

Place any ICS-OS specific shims here (stdio, term handling, filesystem wrappers).

The BusyBox `vi` applet expects a POSIX-ish environment. We'll adapt it to the ICS-OS SDK here.
